console = files('console.c')
console_lib = library(
  'console',
  console,
  include_directories : inc
)

input = files('input.c')
input_lib = library(
  'input',
  input,
  include_directories : inc
)
