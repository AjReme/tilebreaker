console = files('console.c')
console_lib = static_library(
  'console',
  console,
  include_directories : inc
)

input = files('input.c')
input_lib = static_library(
  'input',
  input,
  include_directories : inc
)
